name: CI Pipeline - Step 1

# Déclenchement du workflow à chaque push sur main
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Étape : récupérer le code du dépôt
      - name: Checkout repository
        uses: actions/checkout@v3
